# Cycle 54 - `native-desktop-package-1`

> Drafted 2026-06-03 after Cycle 53 and `v2.2.0` closed. Closed and archived 2026-06-04. Cold-start agents should read [`../../../NEXT_SESSION.md`](../../../NEXT_SESSION.md) first, then this archived plan only for completed scope and acceptance history.

## Goal

Turn the green Cycle 53 Windows Electron proof into the first distributor-grade desktop path for SDS. By cycle close, SDS shall build a Windows Electron package with real app identity, icon, installer and portable targets, signing-ready posture, log/crash paths, packaged-shell gameplay proof, explicit WebGL/WebGPU proof, and a Steam preparation handoff that names what is still missing before store submission.

## Closeout status - 2026-06-04

Phases 1 through 4 shipped. The close commit `d9da08a` was pushed to `main`, and the GitHub Deploy workflow completed successfully. The packaged WebGL runtime proof is green. The packaged WebGPU runtime proof is green on this Windows host. The desktop package proof also verifies native window resize: Electron content size changes to `1040x640`, the page viewport follows it, the canvas matches the viewport, and the camera aspect matches the resized window.

Current package outputs exist under `cycle54-validation/desktop-electron/artifacts/`:

- `win-unpacked/Sheep Dog Simulator.exe`
- `SheepDogSimulator-2.2.0-setup-x64.exe` - 242,122,782 bytes
- `SheepDogSimulator-2.2.0-portable-x64.exe` - 218,431,930 bytes

Current renderer proof:

- `proof:webgl`: pass. `desktop-electron-proof-webgl.json` records `ok=true`, `packaged=true`, `protocol=sds://app`, WebGL renderer, nonblank gameplay, fullscreen, resize, pointer lock, audio unlock, storage persistence, Worker health, and SDS WebSocket proof.
- `proof:webgpu`: pass. `desktop-electron-proof-webgpu.json` records `ok=true`, `effective=webgpu-production`, `devicePreflight.ok=true`, `rendererReady=true`, `rendererIsWebGpu=true`, no fallback, nonblank gameplay, fullscreen, resize, sheep startup motion, and zero fatal console errors.

Steam handoff result: local Steam depot dry-run is plausible if that scope is accepted. Public Steam submission remains no-go until signing decision, metadata, install/uninstall QA, screenshots/capsules, controller notes, cloud-save policy, and release-channel policy are resolved.

## Resolved questions

1. **Q1: Electron Forge or electron-builder?** Resolved: electron-builder for this cycle because it directly supports Windows `nsis` and `portable` targets, target-specific artifact names, app metadata, and Windows signing knobs without replacing the core Vite web architecture.
2. **Q2: Signed now or signing-ready now?** Resolved: signing-ready now. Local validation builds unsigned artifacts with auto-discovery disabled; public release waits for an explicit code-signing certificate or unsigned-release decision.
3. **Q3: Steamworks SDK now?** Resolved: no. First prove plain packaged play, logs, installer/portable behavior, and Cloudflare WebSocket multiplayer. Steamworks features come after depot dry-run.

## Architecture / shared changes

No deterministic `shared/` changes are authorized. The desktop package lives under `native/desktop-electron/` and consumes the existing `dist/` generated by `BUILD_TARGET=native vite build`. The game still boots from web assets over `sds://app`; there is no source dev server coupling and no renderer-default policy change.

## Phase 1 - Distributor Scaffold (~2hr)

**Independently testable.** Converts the isolated proof shell into a real package path without touching the web runtime.

1. Add `native/desktop-electron/` with Electron 42, electron-builder, app identity, Windows icon preparation, `sds://app` protocol, app user model id, and root npm scripts.
2. Configure Windows `nsis` installer and `portable` artifact targets with deterministic artifact names.

**Acceptance (EARS):**

- When `npm --prefix native/desktop-electron run prepare:icons` runs, then `native/desktop-electron/build/icon.ico` and `build/icon.png` shall exist.
- When `npm run desktop:dist` runs, then `cycle54-validation/desktop-electron/artifacts/SheepDogSimulator-2.2.0-setup-x64.exe` shall exist.
- When `npm run desktop:dist` runs, then `cycle54-validation/desktop-electron/artifacts/SheepDogSimulator-2.2.0-portable-x64.exe` shall exist.

## Phase 2 - Runtime Logging, Crash, and Shell Controls (~2hr)

**Depends on:** Phase 1.

1. Add persistent log and crash-dump paths under Electron `userData`.
2. Add full-screen handling, secure BrowserWindow defaults, and production app metadata.

**Acceptance (EARS):**

- When the packaged executable boots under proof, then `cycle54-validation/desktop-electron/userdata-webgl-<timestamp>/logs/sds-desktop.log` shall exist.
- When the packaged executable boots under proof, then `cycle54-validation/desktop-electron/userdata-webgl-<timestamp>/crash-dumps/` shall exist.
- When the desktop proof toggles fullscreen, then the proof JSON shall record `fullscreen.entered=true` and `fullscreen.exited=true`.

## Phase 3 - Packaged Runtime Proof (~3hr)

**Depends on:** Phase 2.

1. Replace proof-only validation with a packaged distributor proof that checks gameplay, renderer mode, artifact names, signing posture, storage, input, audio, logs, Worker health, and authenticated SDS WebSocket.
2. Preserve explicit WebGL and WebGPU packaged runs from Cycle 53.

**Acceptance (EARS):**

- When `npm --prefix native/desktop-electron run proof:webgl` runs after `desktop:dist`, then `cycle54-validation/desktop-electron/reports/desktop-electron-proof-webgl.json` shall record `"ok": true`.
- When `npm --prefix native/desktop-electron run proof:webgpu` runs after `desktop:dist`, then `cycle54-validation/desktop-electron/reports/desktop-electron-proof-webgpu.json` shall record `"ok": true` on this WebGPU-capable Windows host.
- When the proof JSON is written, then it shall record `workerHealth.ok=true` and `webSocket.ok=true`.
- When the proof JSON is written, then it shall record `audio.resumed=true`, `pointerLock.locked=true`, `storage.beforeReloadValue="before-reload"`, and `gamepad.apiAvailable=true`.

## Phase 4 - Steam Handoff and Docs (~1hr)

**Depends on:** Phase 3.

1. Update NEXT_SESSION and docs index to reflect Cycle 54's closeout and handoff state.
2. Add a desktop package proof report and Steam prep checklist deltas.

**Acceptance (EARS):**

- When Phase 4 ships, then `docs/native-desktop-package-cycle-54.md` shall name the generated Windows installer and portable artifact paths.
- When Phase 4 ships, then `docs/native-store-steam-readiness-checklist.md` shall include the desktop package deltas from Cycle 54.
- When Phase 4 ships, then `NEXT_SESSION.md` shall point cold-start agents to Cycle 54 status and next handoff.

## Dependencies

```
Phase 1 -> Phase 2 -> Phase 3 -> Phase 4
```

## Frozen files (cycle-specific additions)

No additional frozen files. Durable frozen files in [`../../INTERFACE_FENCE.md`](../../INTERFACE_FENCE.md) still apply. `shared/` and sim-baseline goldens are not authorized for this cycle.

## Hard stops

1. If WebGL packaged proof fails after artifact build, stop and fix the desktop shell before attempting WebGPU.
2. If WebGPU packaged proof falls back on this Windows host, record the fallback explicitly and do not claim true desktop WebGPU readiness.
3. If Worker WebSocket proof fails due to production auth or rate limiting, do not weaken auth; record the failure and retry with a clean proof identity.

## What NOT to do during this cycle

- Do not add Steamworks SDK.
- Do not change renderer defaults.
- Do not change `shared/` or regenerate sim-baseline goldens.
- Do not rewrite the Cycle 53 sandbox proof into production code; use it as reference only.
- Do not claim public Windows release readiness until signing and Steam metadata are resolved.

## Success criteria (cycle close)

- [x] When the cycle closes, all phases shall be shipped or explicitly deferred to next cycle's `BACKLOG.md` carryover.
- [x] When `npm run lint` runs at cycle close, the deterministic shared boundary shall pass.
- [x] When `npm test` runs at cycle close, all vitest specs shall pass.
- [x] When `npm run build` runs at cycle close, production build shall be clean.
- [x] When `npm run desktop:dist` runs, Windows installer and portable artifacts shall be generated.
- [x] When `npm --prefix native/desktop-electron run proof:webgl` runs, the packaged WebGL desktop proof shall pass.
- [x] When `npm --prefix native/desktop-electron run proof:webgpu` runs, the packaged WebGPU desktop proof shall pass on this Windows host.
- [x] When the close commit lands on `main`, sheepdogsim.com deploy shall succeed via GH Actions if the branch is merged to `main`.

## References

- [`../../native-shell-proof-cycle-53.md`](../../native-shell-proof-cycle-53.md) - prior native shell proof
- [`../../native-store-steam-readiness-checklist.md`](../../native-store-steam-readiness-checklist.md) - store gates
- [`../../CYCLE_TEMPLATE.md`](../../CYCLE_TEMPLATE.md) - this template
- [`../../INTERFACE_FENCE.md`](../../INTERFACE_FENCE.md) - durable frozen files
- [`../../EMERGENCY_STOPS.md`](../../EMERGENCY_STOPS.md) - durable hard-stop list
- [`../../NEXT_SESSION_CONTRACT.md`](../../NEXT_SESSION_CONTRACT.md) - pickup-state contract
