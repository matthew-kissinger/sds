# Node.js dependencies
server/node_modules/
server/package-lock.json

# Server build artifacts  
server/.fly/
server/Dockerfile
server/.dockerignore

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
log/
browserstack-artifacts/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# nyc test coverage
.nyc_output

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.*.local

# Wrangler local-dev secrets (prod secrets live in `wrangler secret put`)
.dev.vars
worker/.dev.vars

# Wrangler local cache + state (workerd, D1 sqlite shadow, KV, etc.)
.wrangler/
worker/.wrangler/

# SQLite database files (live data - never commit)
*.db
*.db-shm
*.db-wal

# VS Code
.vscode/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp 
# Claude Code: shared agent infrastructure (commands, hooks, rules, skills, settings.json) commits with the repo. Only personal/local state stays out.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/projects/

# Codex local state
.codex/

# Playwright MCP artifacts
.playwright-mcp/

# Playwright test artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Cinematic capture intermediates (regenerable via `npm run cinema`)
tools/cinematic/output/
# Playtest screenshot captures (regenerable via `node tools/probe.mjs` /
# `node tools/playtest-screenshots.mjs`)
tools/playtest/
# Marketing video output (too large for git; ship via CDN if needed)
assets/marketing/videos/
assets/marketing/content/*/generated/
examples/capture-spike/repos/

# Build output
dist/
dist-react/

# itch.io build artifacts
dist-itchio/
sheep-dog-sim-itchio.zip

# Playtest screenshots (ephemeral)
cycle2-*.png
phase-b-*.png
phase2-*.png
phase3-*.png
prod-*.png
verify-*.png
verify[0-9]*-*.png

# GLB compression backups (regenerable from git history if rolled back)
assets/_originals/
TODO.local.txt

# Cycle 15 Phase 1: AI asset generation staging (large GLBs, intermediates).
# Picks file IS committed; staging is regenerable via `npm run gen:meshy`.
tools/asset-gallery/staging/
tests/perf-baseline/last-run.json
